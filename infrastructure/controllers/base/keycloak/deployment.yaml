apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
spec:
  instances: 1
  ingress:
    enabled: false
  http:
    httpEnabled: true
  hostname:
    strict: false
    strictBackchannel: false

