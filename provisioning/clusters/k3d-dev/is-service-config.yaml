apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: is-service-config
  namespace: flux-system
spec:
  interval: 1h
  retryInterval: 1m
  timeout: 5m
  dependsOn:
  - name: is-operator-config
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./provisioning/infrastructure/service-config
  prune: true
  wait: true
